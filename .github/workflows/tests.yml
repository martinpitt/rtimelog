name: tests
on:
  push:
  pull_request:
  schedule:
  - cron: 0 6 * * MON,FRI
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions: {}
    steps:
      - uses: actions/checkout@v5
      - run: test/run-container.sh
